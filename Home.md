---
type: home
title: 独立开发项目知识库 · Home
updated: 2026-09-20
tags:
  - 索引
---

# 独立开发项目知识库（Indie Project KB）

> [!tip] 30 秒上手
> **这是证据库不是结论库**：每天自动采集 13 个渠道的独立开发项目语料（全文 + 评论 + 出处），
> 供 AI 做痛点 / 定价 / 增长分析。**第一次来 → 点 [[浏览|从这里开始]]**，按「你想干什么」路由。

## 📊 今日仪表盘

| 在库语料 | 项目页 | 可用率 | 渠道 |
|---:|---:|---:|---:|
| **1615** | **1415** | **88%** | **13 启用** |

> 数字随采集滚动，实时口径以 [[浏览]] 顶部为准。健康自检：八项不变量全绿（`kb_healthcheck.py`）。

## 🧭 我要……

| 想干什么 | 去哪 |
|---|---|
| **找项目 / 看什么在变热** | [[项目地图]] · 最新 [[报告总览\|洞察报告]] |
| **读某条内容的原文和评论** | [[浏览]] → 项目页「观测历史」表 → 语料页 |
| **看某个项目的全部历史** | [[项目地图]] → `10-项目/` 该页观测历史表 |
| **搜痛点 / 定价线索** | Obsidian `Ctrl+Shift+F`，限定 `path:20-语料`，搜「定价 / MRR / 付费」 |
| **查渠道能不能采 / 为什么空** | [[渠道台账]] |
| **捞回被判废的内容** | [[归档与申诉]] |

## 它解决什么问题

| 常见困境 | 本库的答案 |
|---|---|
| 看了一堆项目，回头找不到出处 | 每条语料强制带 `url` / `source` / `captured_at` |
| 平台内容会被删、会改 | 正文与评论**落本地 markdown + JSONL**，不依赖回看原站 |
| 笔记散落在浏览器收藏夹 | Obsidian vault：可 backlink / 可全库检索 / 可被 AI 直接读 |
| 想按不同维度看，得反复翻文件夹 | **[[浏览]] 一页切 8 种视图**（Bases 数据库视图，右上角下拉切换） |
| 从一条语料出发无处可去 | 每条语料页底部有**导航段**（回项目页/人物页/渠道页），不再是死胡同 |
| 不知道该搜什么渠道 | `渠道台账` + `_meta/channels.yaml` 单一事实源，每次运行前体检 |
| 判断「有没有变热」靠感觉 | `10-项目/` 每个项目页有**观测历史表**（指标随时间） |

## 目录结构

**`00-索引/` 分两层**：根目录只放**导航**，生成物进 `报告/`。
（不分的话，索引目录会被带时间戳的报告淹没，就不再是索引了。）

| 目录 | 放什么 | 谁写 |
|---|---|---|
| `00-索引/` | **导航层**：[[浏览|从这里开始]]（总入口，首屏按意图路由）、[[项目地图]]（1389 个项目按赛道）、[[归档与申诉]]（捞回入口）、[[命名与目录规范]]（命名+框架的唯一规范）、[[字段字典]]、[[渠道台账]]、[[运行日志]]、[[待办与决策]]、根目录 `AGENTS.md`（给 AI 的说明） | 脚本 + 人工 |
| `00-索引/报告/` | **生成物**：`洞察-*`、`修复报告-*`、`内容审计-*`、`分析-*`、`存量重判报告`、`实体页归档报告`、`回填队列`、`AgentReach体检`；**先看 [[报告总览]]**（每份一句在讲什么），别按文件名猜 | 脚本自动 |
| `10-项目/` | **在用项目页**：概览 + 观测历史 + 来源语料链接（只剩有在库语料的） | 脚本自动 |
| `20-语料/posts/<渠道>/<日期>/` | **语料主力**：原帖全文 + 全部评论 + 关联链接 + 导航段 | 脚本自动 |
| `30-人物/` | 作者 / 独立开发大牛（maker 维度，`kind=person` 自动分流） | 脚本 + LLM |
| `40-方法论/` | 方法论沉淀：**脚本自动生成 kind=method 实体页**（build-in-public 复盘、收入报告、增长路径 by exa_discovery / c1c7）+ 人工写的独立复盘 | 脚本 + 人工 / LLM |
| `50-渠道/<渠道id>.md` | 每渠道：口径、认证、运行历史、复活条件 | 脚本自动 |
| `80-归档/posts/` | 主题重判后判定为无关的存量语料（**不删**，可按 undo 清单搬回） | `kb_prune.py` |
| `80-归档/项目/` | **已移出检索面的历史实体页**（语料已归档 / 从来不是项目） | `kb_prune.py --archive-entities` |
| `90-原始/<渠道>/<日期>.jsonl` | **不可变原始归档**（含所有规范化字段，供 AI 批量读取） | 脚本自动 |
| `_meta/` | `channels.yaml`（渠道注册表）、`rules.yaml`（主题准入规则）、`schema.json`、`seen.json`、`health.json`、`body_cache.json`、`backfill_dead.json`、`rule_drops.jsonl`（丢弃账本）、`prune_manifest_*.json`、`entity_archive_manifest_*.json`、`rename_manifest_*.json`、`runs/` | 脚本自动 |
| `tools/` | 采集 / 体检 / 诊断 / 回填 / 订正 / 归档 / 洞察 / 存量结构补齐（见下） | 人工维护 |

## 工具链（tools/）

| 工具 | 何时跑 | 干什么 |
|---|---|---|
| `kb_audit.py` | 采集前 | 渠道探活 + agent-reach doctor 对账 → `渠道台账` |
| `kb_collect.py` | 每轮 | 13 渠道取数 → 主题准入过滤 → raw JSONL + 语料页 + 项目/人物页 |
| `kb_analyze.py` | 采集后 | **取数诊断**：按渠道 profile 判定真缺口 → `报告/分析-<run>.md` + 缺口队列 |
| `kb_content_audit.py` | 采集后 | **内容审计**：相关性 / 完整性 / 元数据 / 时效 / 重复 / 分类 → `报告/内容审计-*.md` |
| `kb_backfill.py` | 采集后 | 回填**粘性缺口**（条目离开窗口后永不再取）：正文 + 评论，含死信账本；`--report` 落 `报告/回填队列.md` |
| `kb_prune.py` | 规则变更后 / 每周 | **存量重判归档** → `80-归档/`（含 undo 清单）；`--liveness` 同步实体页 `stale`；`--archive-entities` 把 stale 实体页移出检索面 |
| `kb_reclassify.py` | 按需 | 实体错分订正、孤儿项目页合并、frontmatter 自愈、存量补推 `project_url`、`--reconcile-entities`（按当前 kind 分发规则幂等回放 person/method 实体页 + 清旧盐下的孤儿） |
| `kb_insight.py` | 每周 / 语料大增时 | **洞察层** → `报告/洞察-*.md`；`--browse` 重生成 [[浏览|从这里开始]]（含 Bases 多视图） |
| `kb_moc.py` | 结构变更后 / 每周 | **导航地图（MOC）** → `00-索引/项目地图.md`（1389 个项目按赛道分组）、`00-索引/归档与申诉.md`（归档原因 + 撤销命令）、`00-索引/报告/报告总览.md`（每份报告一句说明）。**为什么放 00-索引/ 而不是数据目录里**：数据目录被计数脚本按 `*.md` glob，塞 index 页会当场污染计数 |
| `kb_navfix.py` | 结构变更后 / 按需 | **存量语料页补齐**：`topic` / `shard` / `pub_day` / `kind` 对齐 / 导航段；`--fix-links` 修搬家后的断链（含「旧名带 wikilink 语法字符、新名被 sanitize」的两代漂移：按去字符化 basename 唯一命中改写）；`--fix-names` 清文件名里的 `[ ] # ^` 与反引号（均幂等） |
| `kb_name_audit.py` | 命名/框架改动后 · 每周 | **命名与目录框架审计（只读）**：wikilink 语法字符、stem 跨目录撞名、大小写碰撞、规范名漂移（与 `--fix-names` 同判据）、顶层目录越界 → `报告/命名审计.md`。**改名只有 `kb_navfix --fix-names` 一条路**，手改必漏 manifest/seen/链接 |
| `kb_healthcheck.py` | 任何改动后 | **五项硬不变量 + 一项软门**：语料数=在库数 · 实体页不缺 · 无缺 url · **断链 0** · **路径可复现 0 漂移**；软门 ⑥：孤立语料（无入链）报数但不阻断收工 |

> **两道门禁**：`kb_audit` 管「能不能取到数」，`kb_content_audit` 管「取到的是不是想要的」。
> 二者正交 —— 取数缺口为 0 不代表内容可用（修复前实测：取数缺口 2 条，但可用语料只有 51%）。
>
> **一道纪律**：判定前先分「内容问题还是渠道语义问题」。契约型渠道（只收产品的站）
> 不因「缺关键词」判无关；话题型渠道必须靠关键词，且**只匹配标题**（长正文里顺带提到
> launch/startup 不算数）。详见 skill 的「判定纪律」段。
>
> **一条结构纪律**：语料页必须带**出链**（导航段）。只靠文件树找东西，
> 等于把「检索」退化成「翻目录」—— 实测 723 条语料页曾**一条 wikilink 都没有**。

## 当前状态（2026-09-20）

**取数层面（健康）** · 自动化当日又跑过一轮，数字随采集滚动

- 在库语料 **1615** · 可用 **1415（88%）**，其中**项目型** 以 [[浏览]] 实时统计为准 · 孤儿项目页 **0** · 人物页以 `30-人物/` 为准 · **方法论页** 以 `40-方法论/` 为准
  （可用 ≠ 是项目：讨论帖/经验帖也是好语料，但不能当项目算 → 单列，防误用）
- **检索面已收窄**：`10-项目/` 在用 **1389** 个；历史实体页（语料已归档 / 从来不是项目）已移进 `80-归档/项目/`，带 undo 清单
- 回填队列（缺正文但**要保留**的项目）以最新 [[报告总览]] 内的分析报告为准（每轮重建、带账龄）
- **不变量（九门）**：① 语料数=在库数 · ② 实体页不缺 · ③ 缺 url **0** · ④ 断链 **0** · ⑤ 路径漂移 **0** · ⑥ 孤立语料（软门）· ⑦ project_url 合法（软门）· ⑧ 共用页（软门）· ⑨ 单渠道存量占比（软门，hn_show 现超门槛）
- 启用渠道 **13**，停用各条附原因与复活条件 · 未解锁 1（X；小红书已解锁，2026-09-21）

> 数字随采集滚动，**以 [[浏览]] 顶部的实时统计为准**（本页不追每日波动）。

**内容层面（已从 51% 治到 86%）** → 详见 [[修复报告-20260920]]

- 正文中位长度 **1936**（修复前 927）· Exa 截断 **0** · RSS 残文 9
- 内容发布时间轴：**670 条**带 `published_at`，跨 90+ 天 → 趋势类分析自此成立
- 残留问题：无指标 · 无项目链接 · HTML 残留 · 部分缺发布时间；
  跨渠道强信号偏少（受 `project_url` 对 PH/BetaList 结构性不可得所限）

## 怎么用

**给人看**

```
Obsidian 打开 → [[浏览|从这里开始]]
  ├─ 第 0 节「你现在想干什么」—— 6 类问题各一个落点（找项目/读原文/看趋势/复核归档/理解规矩/查渠道）
  ├─ 第 1 节「五个数字」—— 在库/项目页/报告/归档/渠道
  ├─ 第 2 节　Bases 数据库视图（按赛道/渠道/发布日/批次/项目池/人物/渠道健康，下拉切换）
  └─ 第 3~5 节　静态速查表（不依赖插件，点即达）
想按赛道浏览项目 → [[项目地图]]（1389 个项目的分组地图）
想知道判废了什么、怎么捞回 → [[归档与申诉]]
报告太多不知道读哪份 → [[报告总览]]
从任一条语料页底部的「导航」段可以跳回它的项目页/人物页/渠道页
```

**给 AI 读**（不碰 Obsidian UI，直接喂）

```bash
# 0) 先读根目录 AGENTS.md —— 库的分层、事实源地位、口径与禁忌都在那一页
# 1) 全库原始数据（单渠道当日全量）
type 90-原始\hn_show\2026-09-20.jsonl
# 2) 先读判断，再读原始数据
#    00-索引\报告\洞察-20260920.md      ← 结论先行 / 赛道 / 强信号
#    00-索引\报告\修复报告-20260920.md  ← 已修什么、还剩什么
# 3) 按项目看演化 → Obsidian 打开 10-项目/，看「观测历史」表
#    （不知道项目名时先读 00-索引\项目地图.md —— 按赛道分组的地图）
# 4) 按渠道看健康 → 00-索引/渠道台账.md · 00-索引/报告/内容审计-all.md
```

## 运行方式（混合：脚本采集 + skill 编排）

| 频率 | 动作 | 命令 |
|---|---|---|
| 每日 | 渠道体检 → 采集 → 诊断 → 回填 → 内容审计 | `kb_audit.py` → `kb_collect.py` → `kb_analyze.py` → `kb_backfill.py` → `kb_content_audit.py` |
| 每日 | 收尾：不变量自检（断链/漂移/计数） | `kb_healthcheck.py` |
| 每日 | 由 automation 触发 skill `indie-project-kb`（含渠道增删判断） | 对话里说「跑独立开发知识库」 |
| 每周一 | 全网语义发现 + 存量重判 + 实体页清面 + 洞察 + 浏览页 | `kb_collect.py --force-weekly` → `kb_prune.py --apply` → `kb_prune.py --archive-entities --apply` → `kb_insight.py --browse` |
| 历史铺底 | 一次性把语料回溯 N 天 | `kb_collect.py --days 90 --limit-mult 10 --slice-days 10 --no-enrich` |
| 按需 | 实体订正 / 孤儿页合并 / 页自愈 / 补 project_url | `kb_reclassify.py [--merge-orphans / --repair / --project-url]` |
| 按需 | 存量语料页补结构（导航段/字段） | `kb_navfix.py [--dry]` |
| 按需 | 实体页存活标记 / 移出检索面 | `kb_prune.py --liveness [--apply]` · `kb_prune.py --archive-entities [--apply]` |
| 按需 | 修搬家后的断链 / 清文件名语法字符 | `kb_navfix.py --fix-links [--dry]` · `kb_navfix.py --fix-names [--dry]` |
| 按需 | 回填队列台账 | `kb_backfill.py --report` |

> **回填不是可选项**：采集窗口是滚动的，条目一旦离开窗口，本轮取数失败的正文/评论就**永远不会再被取到**。
> 只跑 `kb_collect` 不跑 `kb_backfill`，缺口会持续累积（实测 R2 遗留 31 条）。

## 设计原则（沿用 find project 的教训）

1. **渠道腐化必须被发现**：每渠道有 `status`，坏掉写进台账而不是静默返回空。
2. **不静默降级**：缺登录态（小红书/X）记 `auth` 状态 + 解锁方式，不假装已覆盖。
3. **原始归档不可变**：分析口径变化只加新字段，不改老 JSONL。
4. **根目录只放活状态**：语料按渠道/日期分桶，避免复现「根目录 168 个文件」。
5. **单一事实源**：改渠道只改 `_meta/channels.yaml`，代码分派靠 `adapter` 字段。
6. **索引层不被生成物淹没**：导航与报告分目录，索引目录里只放"入口"。
7. **每页都要有出口**：任何 note 都不是终点 —— 语料页必须能跳回实体页与浏览页。
8. **搬家必须可逆且有账**：任何 move/rename 都写 undo 清单（`*_manifest_*.json`），
   同步 `seen.json`，并**当场改写指向旧路径的链接**（`kb_navfix --fix-links`）——
   否则「清理」会悄悄变成「制造断链」。
9. **清理完必须过不变量**：`kb_healthcheck.py` 五项全绿才算收工；
   数字对不上就是改动错了，不是「差不多」。**用户看的检索面 = 库里实际有的东西**，
   撤出检索面的东西必须真的离开 `10-项目/`，不能只靠视图过滤。
10. **入口页按「意图」组织，不按「内容类型」**：目录名（`10-项目`/`20-语料`）回答的是
    「这里有什么」，用户问的是「我想干什么」。链接必须在**点开之前**就能判断去哪
    （信息气味）；地图（MOC）负责分组导航，Bases 视图负责条件过滤 —— 查询 ≠ 导航，
    两者都要有。地图只放 `00-索引/`，不进数据目录（避免污染计数）。
11. **文件名是解析键，不是展示名**：wikilink 按文件名解析、采集端按规范路径写盘、
    seen.json 按路径认路 —— 三件事都押在名字上。字符规则（`[ ] # ^` 反引号）、
    全库 stem 唯一、改名只走 `kb_navfix --fix-names`，细节见 [[命名与目录规范]]。

## 相关

- 给 AI 的说明：根目录 `AGENTS.md`（分层 / 事实源地位 / 口径禁忌 / 改库纪律）
- 总入口：[[浏览|从这里开始]] · 项目地图：[[项目地图]] · 归档与捞回：[[归档与申诉]] ·
  报告怎么选：[[报告总览]] · 渠道与口径：[[渠道台账]] · 字段定义：[[字段字典]] · 运行史：[[运行日志]]
- 上游参考实现：`F:\AIProject\find project`（淘金雷达，13 维评分 + PIS）
