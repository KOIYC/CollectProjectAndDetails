---
type: index
title: 运行日志
updated: 2026-09-25
tags:
  - 索引
  - 运行日志
---

# 运行日志

> 每次采集追加一条（由 skill `indie-project-kb` 流程第 5 步写入）。机器可读明细在 `_meta/runs/*.json`。

## 2026-09-25 00:35 · 每日采集（run `20260924T235719`，非周一）+ 三处「口径不同源」修复

**渠道体检**：13 启用渠道 → **ok 11 / empty 0 / error 0 / auth 2**（X、小红书需 OpenCLI 登录态）。`delta`：`jina_reader`（doctor 声明 ok、实测 Tunnel 502）；新增登记 `exa_web_fetch` **免费额度可打满**（本轮 429 → 正文补全整批无效，非渠道故障，勿改渠道参数）。**本轮无渠道启停**。
体检自身修了两处：① `_meta/channels.yaml` 被整段重复追加 3 遍（54 block / 18 id）→ 探针跑 **39 次**、单轮 11min，且 c1c7 的重复副本缺 `params.readme` → 探针报 `unknown url type: 'None'` 被记成 error（假错误）。按 id 归并后 1330→558 行、探针 **13 次**、单轮 5.5min，**假 error 消失**。② 探针把 `subs` 截到 1 个 + `limit=3`，`min_score` 一滤 → reddit 恒报 `empty`，而同轮采集有 19 条 → 阈值型渠道改为保留完整 `subs`（口径同采集），探针后半段 `ok`。

**采集**：12 渠道（`exa_discovery` 是 `weekly_only`，非周一不跑）→ **175 条（新 40）**，写页 151，`write_errors` 0，用时 761s。
丢弃 34 条（github_new 16/28=57% · c1c7 10/17=59% · v2ex 5 · devto 2 · bilibili 1）→ **无渠道过 70% 线**，不触发误杀复核；抽查 github_new 被丢的是 `awesome-*`/`clone`/`directory`/`Examples` 类资源清单，属规则本意。
**reddit 窗口修复生效**：`settle_days 3→10 / window_days 7→14`（证据：0~14 天前每 sub 最新 100 条里 score≥3 的 **0** 条、max=2；而 3~13 天前 88/700、10~17 天前 96/700）→ 本轮取到 19 条（新 2）。

**取数诊断**：在库 **1886** · 唯一 1886 · 缺 url **0** · 待回填 45。
**回填**：队列 80 → 正文补 15/60、评论 58/60；复跑诊断缺口 **45 → 25**。Exa 429 让 3/4 批次失败，剩余缺口已入队列与死信账本（`no_backend` 24 / `unreachable` 19 / `http_403` 2）。
**内容审计（与取数正交）**：可用率 **88%**（1662/1886）≥70% 达标；**无渠道 <50%**（`c1c7` 4/7 为 n=7 小样本，只看不动作）。
**语义 lint**：冲突 1（`aliasfleet` 价格 7 vs 17 —— 上一轮已人工核为分档表述，**已知误报**）· 过期论断 0 · 概念缺页 1 · 缺引用 20。

**结构维护**：`navfix --dry` 65 → apply → **复跑空（幂等）** · `--fix-names` 空 · `--fix-links` 0 改写 · `--liveness` **stale 0** · `--archive-entities` **移出 0**（无页需搬） · `--browse`（在库 1886 · 可用 1660 · 项目型 1473 · 强信号 6 · 变现线索 786/金额证据 281） · `kb_moc`（地图 1614 · 归档语料 117 · 归档实体页 327） · **命名审计全部通过**（审计 4043 note，3504 条做规范名核对）。

**本轮核心：三处「工具口径与它要验证的那一端不同源」**（同一类缺陷 —— 它会**稳定地**给出方向相反的结论，比随机出错更危险，因为会催生错误的流程决策）

| # | 工具 | 错在哪 | 后果 | 修法 |
|---|---|---|---|---|
| A | `kb_navfix --fix-links` | 不剥代码段，而 healthcheck ④ 剥 | 运行日志里「本轮修过哪条断链」的示例 `[[10-项目/store.steampowered.com_6608b43a]]` 被当断链 → 假警报 1 条（上一轮把它记作「保持冻结不改写」） | 扫前先 `strip_code`（等长空白占位 → 用 `m.start()` 判定命中是否落在代码段内）→ **「目标不存在」归零** |
| B | `kb_prune --archive-entities` | 把「页名漂移留下的旧页」当普通 stale | 2 个页被判 stale 且即将归档，实际各自有**本轮仍在库**的语料（`last_seen` = 09-25、note 文件都在盘） | 走 `kb_reclassify --merge-orphans` 合历史（+7 / +2 行观测）→ 旧页删除、规范页保留、**stale 0** |
| C | `kb_audit.probe_channel` | 探针裁参（`subs[:1]` + `limit=3`）而采集不裁 | 探针恒 `empty` → 会误触「连续 2 轮 empty → 降权/停用」 | 阈值型渠道保留完整 `subs`（口径同采集），阈值**不**放松（否则会掩盖「阈值本身设得取不到数」） |

**B 的成因值得记住**：同一项目的 `project_url` 换成了**另一个非空值**（`NandhaKishorM/laya`：仓库链接 → README 里的 HF model 链接；`Tartelo`：betalist 页 → Discord 邀请）→ 页名 hash 变、新页接走在库语料、旧页成孤儿被 liveness 判 stale。**这类旧页不能当 stale 直接归档**（归档 = 静默丢观测历史）。同轮给 `merge_orphans` 补了「语料列必须带链接」：原先传 `None` 会让 `_obs_row` 写出 `—`，并把该时刻**原有那条带语料链接的行替掉** —— 合并是「保历史」的动作，不该顺手降级可追溯性（已用备份前后比对确认）。

**验收**：`navfix --dry` 第二次空 · `fix-links` 复跑稳定且 0 改写 · `fix-names` 空 · healthcheck **五项硬门全绿**（①1886=1886 ②缺页 0 ③缺 url 0 ④**断链 0** ⑤**漂移 0**[盘↔账 孤儿 0 / 幽灵 0]）· 自测 **55/55 OK**（含此前失败的 `test_healthcheck_hard_gates`）。
软门：⑥ 孤立 65（结构性：多为 `kind=post` 讨论帖，按规则不建实体页，渠道页「运行历史」表也不做条级 wikilink）· ⑦ 可疑 0 · ⑧ 共用页 11 组（与 ②「1625 应有 vs 1614 现有」的 11 差值同源，**非缺页**）· ⑨ hn_show 存量占比 59%（条目）/69%（项目页）（历史铺底偏向，靠新增稀释）。

**待办（本轮查实、需独立一轮修）**：**67 个条目的最新记录丢掉了仍可从历史记录取到的 `project_url`**（`repair` 43 / `backfill` 10 / `refresh` 8 / `update` 6；渠道：bilibili 36 · reddit 15 · hn_show 7 · c1c7 5）。成因：`kb_prune.load_items()` 取「**正文最长**」记录（通常有 project_url），而 `kb_analyze.latest_by_item()` 取「**captured_at 最新**」记录（多为 `record_type=repair` 且 `project_url=None`）→ 下游（契约门的「无项目链接」、跨渠道归并键、洞察强信号）看到的是空值。
修法必须**成组**：① 让 `project_url` 在 `latest_by_item` 里只增不减（继承历史非空值）→ ② 这会让 ② 的「应有项目页」把这些条目算进来（它们确实 `is_project_ish`）→ ③ 同轮补建这批实体页，否则 ② 缺页会从 0 变正数。本轮未动（属数据语义变更，需独立验证周期），已记入 skill 已知缺口。

## 2026-09-21 10:30 · 每日采集（run `20260921T094358`，周一）+ 级联伤根因修复

**渠道体检**：13 启用渠道 → **ok 11 / empty 0 / error 0 / auth 2**（小红书已解锁可采 6 条；X 仍需 OpenCLI 登录态）。delta 仅 `jina_reader`（doctor 报 ok、实测 Tunnel 502，已在 `channels.yaml` 的 `backends` 段登记，非新增）。无连续空渠道 → **本轮无渠道启停**。

**采集**：全渠道 + `--force-weekly`（Exa 全网语义发现）→ **173 条（新 100）**。`c1c7` 首轮 timeout，单渠道重试通过（7 条，瞬时故障）。
丢弃账本：`github_new` 64%、`bilibili` 50% —— 均未过 70% 线。`github_new` 18 条核为真废料（awesome-*/directory/clone/journal）。
`bilibili` 模拟加「变现轴」词表可救回 5 条但**仅 1 条真阳**（其余为课程推销/泛副业）→ **不采纳，只记录**（不为提命中率放宽规则）。

**取数诊断**：在库可消费 **1615** · 缺 url **0** · 待回填 **9**（github_new/body 4、reddit/body 3、c1c7/project_url 2）。
**回填**：正文 14/31（github readme 9/9 命中）、评论 57/60 → 复跑诊断缺口收敛，回填队列报告已刷新。

**内容审计（第二道门，与取数正交）**：可用率 **87%**（1413/1615）≥70% 达标。无渠道 <50% → 不触发降权/停用。
`c1c7` 4/7=57%（n=7 小样本，只看不动作）、`reddit` 149/244=61%（50–80% 带内，维持 limit 30）。

**周一链**：存量重判**归档 0**（规则未变）→ `reclassify --merge-orphans` 合并 8 个孤儿页 → `--liveness` →
`--archive-entities` 搬 2 页（`-dup` 副本占位后解锁）→ `--dedupe-archive` 清 97 份冻结区重复副本 →
`insight --browse`（在库 1615 · 可用 1411 · 项目型 1258 · 强信号 5 · 变现 684）→ `kb_moc`（地图 1389 · 归档语料 117 · 归档实体页 327）→ **命名审计 2 → 0 全通过**。

**本轮核心：级联伤（三处静默根因）**
症状：`② 缺页 4 + ④ 断链 4`，且 `--repair-links` 修好后**下一轮 navfix 又写回旧名**。拆解出三条独立根因：

| # | 根因 | 后果 | 修复 |
|---|---|---|---|
| A | `kb_reclassify.merge_orphans` 删孤儿页但**不改写指向它的链接** | 语料导航段仍指旧名 | 加链接改写；新增 `--repair-links`（幂等，路径不带 `.md` 后缀） |
| B | `kb_prune._live_stems` **盲信语料导航段**（该段被 A 污染） | 旧名被当「在库」、规范新名被判 stale → `--archive-entities` 把**在库页搬进归档** | 改以 `seen` 记录 note 为准（与 healthcheck ② 同源）+ 排除归档区 |
| C | `kb_navfix` 从语料 frontmatter 重建记录，**frontmatter 无 `project_url`** | nav_block 退回 url 哈希算实体页名 → 与 ② 算出的名字不同，互相对打 | 加 `live` 同源：`rec["project_url"]` 由 live 记录补齐（**只在内存，不写 frontmatter**） |

**现状修复**：部分 undo（`_meta/partial_entity_undo_20260921T100057.json` 搬回 4 个在库规范页）→ `--repair-links` → 复跑幂等为空 → `navfix` 幂等（第二次空）→ `dedupe-archive --prune-empty-dirs` 幂等。

**验收**：`navfix --dry` 4 → 0（幂等）· `fix-links` 改写后复跑稳定 · `fix-names --dry` 空 · healthcheck **八项全绿**
（①1615=1615 ②缺页0 ③缺url0 ④**断链0** ⑤**漂移0**[盘↔账 孤儿0/幽灵0] ⑥孤立22[软门] ⑦可疑0 ⑧共用9组正常）。
`fix-links` 残留 1 条「目标不存在」= `00-索引/运行日志.md` 对已归档页的历史引用（该页 alias 仍在 → ④ 按 alias 解析成功，非断链），**保持冻结不改写**。

**决策记录（新增）**：④ 级联伤的正确治法 = **让导航段与 healthcheck 用同一个事实源（live 记录）**；只要有一端从 frontmatter 反推，两端就会对打，表现为「修好又坏」的振荡 —— 这类振荡型断链要先查「谁在回写」，而不是反复跑修链工具。

## 2026-09-21 · 历史铺底补采（--since 2026-04）+ 正文缺口清零 + 双后端

**铺底**：`kb_collect --since/--until --limit-mult 3 --no-enrich` 按 2026-04~08 窗口补采
 → 在库唯一条目 **707 → 1515**，raw 记录 3491 → 4863。发布轴深度：近 90 天 1050 · 更早 371 · 无发布时间 94。
**注意**：发布轴铺开了，**采集轴仍只有 2 天** —— 趋势/增长率类判断依旧不成立。

**正文缺口 603 → 31**：三轮 `kb_backfill`（289 + 246 + 16），评论回填 262/266 + 61/64。
本轮关键改动 = **正文取数双后端**：新增 `kb_common.direct_fetch_texts`（stdlib urllib 直取，8 并发，
主机级失败同轮拉黑）**优先**，`exa_fetch_texts` 降为**兜底**。
原因：Exa 免费额度打满返 429 时整轮补全全废（534 条 hn_show 卡死）；实测直连对 project 站 6/6 命中。

**可用率 51% → 88%**（可用 779 → 1330 / 1515）。剩余问题标签：无项目链接 237、无指标 158、
缺作者 106、HTML 未清理 82、视频无正文 77（结构性，记死信）。

**修掉的 3 个静默 bug**
1. `kb_render` 跨零点用 `captured_at` 当分片 → 越位新建 52 份重复语料页（① 当场破功）。
   改为「语料页自己的日期桶优先 + 写前算目标路径，不一致就跳过不写」。
2. `Exa 429` 空返回被当成「这条没正文」静默跳过 → 现在响亮区分 429 与真失败。
3. `healthcheck ④` 把**外站正文里的 `[[...]]`** 当链接扫（讲 Obsidian 的 Show HN 正文里
   `[[keys.command]]` 是内容不是链接）→ 语料页只扫 frontmatter + 导航/关联链接段，误报归零。

**结构维护**：`--liveness` 标 stale 95 → `--archive-entities` 移出 94（1 个被报告页自引用死锁，
确认归档区已有同 stem 同 hash 副本后移入 `80-归档/重复副本/`）。
**收工门**：自测 44/44 · 命名审计全部通过 · ①-⑤/⑦ 全绿 · ⑥ 孤立 1 条（软门）· 已 commit 并推送。

## 2026-09-21 · project_url 守卫 / frontmatter 往返 / 命名审计范围（三个静默 bug + 一处范围决策）

**触发**：全库问题分析第二轮 —— healthcheck ④ 断链 3 条 + ⑦ 可疑 `project_url` 1 条 + 命名审计 52 处误报。

| # | bug | 症状 | 根因 | 修复 |
|---|---|---|---|---|
| 1 | `project_url` 守卫被 `www.` 绕过 | 讨论页/聚合站被当成项目官网写进 `project_url`（= 项目页文件名 + 跨渠道归并键） | `project_url_reject` 直接拿 `netloc` 比 `NON_PROJECT_HOSTS`，`https://www.bilibili.com/…`、`https://www.producthunt.com/products/…` 这类带 `www` 的**全部放行**（实测 5/5 漏网） | host 先 `removeprefix("www.")` 再比对（本机地址/无顶级域/讨论站/博客托管/通用站根路径五条判据同步） |
| 2 | `LINK_RE` 吞 markdown 转义 | reddit 正文 `[https://www.producthunt.com/products/x?utm\_source=other](…)` 被整段抓成 URL，`norm_url` 再把 `\` 编码成 `%5C` 写进 `project_url`（1 条 live 脏值） | 字符类 `[^\s<>\)\]\"']` 未排除反斜杠 | 字符类加 `\\` |
| 3 | frontmatter 读写不对称 | ④ 断链 3 条：`[[10-项目/store.steampowered.com_6608b43a]]` 指向不存在的页（真页 `_49b18d5d`） | `write_note` 用 `json.dumps` 写双引号标量，`fm_scalars` 读侧只 `strip('"')` —— `project_url` 带 `\` 时往返**放大成两个反斜杠**，与账本算出两个不同的项目页 hash，navfix 于是写出死链 | 双引号标量走 `json.loads` 解码；非法 JSON 才退回朴素去引号 |

**自测 38 → 44 例**：新增 `ProjectUrlGuard`（www 变体必须被拒 / 真项目站 www 变体必须通过 / `LINK_RE` 结果不得含 `\`）、`NameAuditScope`（暂存区排除但真归档仍查）、`FrontmatterRoundtrip`（反斜杠 + 引号往返不变形 / 裸值仍可读）。

**范围决策**：命名审计**整体排除 `80-归档/重复副本/`** —— 它是 `kb_prune --dedupe-archive` 的「只移不删」undo 现场（必然是 live 页同 stem 副本），参与「同 stem 多目录」检查会**稳定误报**（实测 52 处），与「冻结区只查语法字符」的既有规则自相矛盾。`80-归档/posts|项目|野页` 仍照常查语法字符。

**存量订正**：`kb_reclassify --project-url`（dry 报 283 → apply 实改 7 条）→ healthcheck ⑦ 1 → 0。

**结构维护（周一链）**：`--liveness --apply`（stale 5，摘除 0）→ `kb_moc` → `--archive-entities --apply` → `--dedupe-archive --prune-empty-dirs --apply`（去重 0）→ `kb_insight --browse` → `kb_navfix`（写导航 190 / 补 topic 187；幂等复跑为空）。

**验证（终跑）**：自测 **44/44** · healthcheck **①-⑤/⑦ 全绿**（①1515=1515 ②缺页 0 ③缺 url 0 ④断链 0 ⑤漂移 0 ⑦可疑 0）· ⑥ 孤立 **1 条**（软门：非项目型讨论帖无实体页、无入链，属结构现象）· ⑧ 共用 7 组 14 条（正常）· **命名审计清零**。

**delta**：`kb_prune --dedupe-archive --prune-empty-dirs --apply` 打印 `[!] 删不掉 20-语料/posts/c1c7/2026-09-21: [WinError 2]`，但两个目录实测已不存在 → **计数假阴性**（候选目录已被前一步清掉），未改代码。

## 2026-09-20 · 建库 + 首次种子采集

**渠道**：17 个启用（13 零认证 OK + 2 待解锁 + 2 观察）
**语料**：272 条唯一条目（items）· 正文覆盖率 **89%** · 880 条评论 · 缺 url 0 条 · 落盘 1.2 MB 语料 + 1.7 MB 原始归档

| 渠道 | 条数 | 正文 | 评论 | 状态 |
|---|---|---|---|---|
| HN Show HN | 41 | 链接帖补外链正文 27/27 | 75 | ok |
| GitHub 新星仓库 | 28 | README 全文 | — | ok |
| Reddit 独立开发版块 | 23 | 自帖正文 + 外链正文 | 532 | ok |
| Lobsters | 20 | 外链正文 20/20 | 119 | ok |
| dev.to | 5 | body_markdown | 121 | ok |
| Product Hunt | 30 | feed 全文 + Exa | — | ok |
| Indie Hackers 产品库 | 18 | Exa 正文 | — | ok |
| BetaList | 13 | Exa 正文 12/12 | — | ok |
| App Store 榜单 | 15 | 元数据 | — | ok |
| V2EX（sov2ex） | 25 | 正文含 content | 回复数 | ok |
| 1c7 中文名录 | 17 | 名录描述 | — | ok |
| 少数派 | 10 | feed 全文 | — | ok |
| B站 | 12 | 简介 | — | ok |
| 小红书 | 0 | — | — | **auth（未解锁）** |
| X / Twitter | 0 | — | — | **auth（未解锁）** |
| Uneed | 0 | — | — | **empty（Exa 抓不到，待评估停用）** |

**质检门**：缺 url 0/0 ✅ · 有正文 **89%** ≥70% ✅ · 新条数 272 在 100-300 ✅ · 单渠道最大占比 15% ≤50% ✅

**本轮踩到并修掉的问题**（都已写进代码注释 / skill 失败处理表）：

1. **cli 解析**：npm 的无扩展名 sh shim 在 subprocess 里 not-found → `kb_common.which_cli()` 解析 `.CMD`/`.EXE`。
2. **Jina Reader 不可用**（doctor 却声明 web=ok）：`r.jina.ai` 502/HTTP 000 → 通用正文改 Exa web_fetch。
3. **arctic `after` 参数**：带时区 ISO 会 400，只吃 epoch 秒/纯日期。
4. **Reddit score 装载延迟**：近 3 天帖 max=1、7-14 天前 max=42 → 改 `settle_days=3` 沉淀窗口。
5. **arctic 评论 `limit` 上限 100**：传 800 直接 400，异常被吞 → 改 `after` 游标翻页。
6. **`ENRICH_ROUTING` 键写成 adapter 名**（`reddit_arctic`）而非渠道 id（`reddit`）→ Reddit 评论补全静默失效；已加注册表自检。
7. **Apple feed 的 genres 是 dict** → tags 强制字符串化 `as_tags()`。
8. **gh search 的 JSON 字段名**：无 `repositoryTopics`/`primaryLanguage`，改用 `language` 等合法字段。
9. **链接帖正文在外部页**：加 `extra.fulltext_url` 路由（HN 外链 / Lobsters story_url / Reddit 外链）。

**渠道决定**：

| 动作 | 渠道 | 理由 |
|---|---|---|
| 停用（登记） | Fazier | `/launches/` 404 |
| 停用（登记） | HuggingFace | 本网络 502 |
| 停用（登记） | DecoHack | RSS 长期无增量（沿用 find project 结论） |
| 停用（登记） | IH-Algolia | 公开 key 403 |
| 待评估 | Uneed | Exa 抓不到正文，长期 empty |
| 待解锁 | 小红书 / X | 需 OpenCLI 扩展或 Cookie |

**Delta 记录**：`jina_reader` — agent-reach doctor 声明 `web=ok`，实测 unusable（Tunnel 502 / HTTP 000），已写入 `_meta/channels.yaml` 的 `backends` 段。

**下一步**：解锁社媒 → 补 `30-人物` / `40-方法论` 聚合 → 上语料分析管道（痛点聚类 / 定价分布 / 上线节奏）。

---

# 五轮连跑（R1~R5）· 每轮「采集 → 分析 → 优化」

目标：验证采集闭环在**连续运行**下是否稳定，并把每轮诊断出的问题就地修掉。

## 各轮数据

| 轮 | run_id | 条数 | 新增 | ok 渠道 | 耗时 | 唯一库量 |
|---|---|---|---|---|---|---|
| 种子 | `20260920T023532` | 268 | 268 | 14 | 273s | 268 |
| R1 | `20260920T025555` | 252 | 6 | 13 | 422s | 282 |
| R2 | `20260920T030427` | 272 | 31 | 14 | 447s | 309 |
| R3 | `20260920T031651` | 273 | 12 | 14 | 507s | 321 |
| R4 | `20260920T032909` | 272 | 8 | 14 | 458s | 329 |
| R5 | `20260920T033847` | 271 | 5 | 14 | 384s | 334 |

同日连跑的「新增」自然衰减（R1 6 → R5 5），这不是故障：`seen.json` 内容指纹让重复条目只记观测、不重复写页。
累计 **记录 1824 条 / 唯一条目 334 条 / 缺 url 0 / 收入信号 56**。

## 每轮诊断 → 优化

### R1：幂等性 + 6 处口径缺口

- 现象：同日重跑「新增 0、重复率 97%」，全部 note 被重写。
- 根因：`record_type` 只看 `seen` 是否命中，没比对**内容指纹**。
- 修复：`Seen` 加 `content_hash`；`record_type` 分 `new / update / refresh`；仅 `_changed` 才写盘。
- 一并修：`ENRICH_ROUTING` 键错（adapter 名 → 渠道 id，Reddit 评论静默失效）、链接帖正文路由到 `fulltext_url`、
  Lobsters/HN 评论树、Apple `genres` dict、`gh search` 非法字段、新增 `hn_front` 渠道、停用 `uneed`。

### R2：QA 阈值数据驱动化 + 粘性缺口回填

- 现象：分析器把 PH / IH / BetaList / 少数派 / Exa 全标成「无项目链接」「正文<70%」——**全是误报**。
- 根因：QA 用统一阈值硬套所有渠道，而链接型/元数据渠道本来就不该有正文或项目链接。
- 修复：注册表加 `profile`（discussion / linkpost / project / metadata / discover），`kb_analyze` 按画像判定，误报清零。
- 新发现：**缺口是粘性的** —— 条目一旦离开采集窗口（github 14 天、reddit 7 天），取数失败的条目**永远不会再被重取**。
- 新工具：`tools/kb_backfill.py`（github 走 gh readme，其余走 Exa）。实测 31 条缺口 → 9 条（github readme 13/13 全中）。

### R3：P0 —— 回填的正文被后续轮次抹掉

- 现象：R2 刚回填好的 13 条 github 正文，R3 又全部变成缺口。
- 根因：条目**回榜**但本轮取数失败 → 新记录 `body` 为空且 `captured_at` 更晚 → `latest_by_item` 取到空版本，覆盖历史正文。
- 修复：`kb_common.BodyCache`（`_meta/body_cache.json`，只增不减）；采集端「本轮回榜但取不到 → 继承历史正文」。
- 验证：R4 日志 `正文继承 12 条`、R5 `正文继承 99 条`——数据不再丢，且不重复烧 Exa 额度。
- 一并修：
  - **dev.to 评论静默截断**：`/api/comments` 默认 `per_page=30`，182 评论的大帖只拿 30 条。改为 `per_page=60` + 翻页，
    抓取率 29% → **100%**（182/182）。
  - **死信账本** `_meta/backfill_dead.json`：视频帖（`v.redd.it`）、社交账号页（`x.com/<handle>`）结构性无正文，
    命中即记账不重试；其余连试 2 轮取不到也记账。分析器自动排除，避免每轮白烧额度。
  - **可观测性**：分析器新增 `正文错 / 评论错 / 翻页中断` 列——静默失败必须可见。
  - **人物分流**：1c7 名录里的「独立开发大牛」（`Patrick McKenzie (@patio11)` 等）`kind=person` → `30-人物/`。
  - **存量订正** `tools/kb_reclassify.py`：3 条错分从 `10-项目/` 搬到 `30-人物/`。

### R4：评论也会粘性缺失 + 项目页孤儿

- 现象一：dev.to 的评论在条目离开窗口后同样不会再补。
- 修复：`kb_backfill` 增加评论通道，直接复用各渠道 `ENRICH_ROUTING` 的 enrich 函数（R4 补 3/3、R5 补 4/4）。
- 现象二：`10-项目/` 有 20 个**孤儿页**（URL 与当前页不同源）。
- 根因：项目页文件名 hash 用 `project_url`，而它靠「正文里第一个非同源外链」回退推导 —— 本轮正文取不到 → 退化成 None → key 变 → 新建页、旧页成孤儿。
- 修复：① 采集端 `project_url` **只增不减**（持久化在 `seen.json`，与本轮解析不到时继承）；② `kb_reclassify --merge-orphans` 合并存量。
- 一并修：`make_item` 回退会选中 `github.com` / `localhost:8080` 等**裸域名**当项目链接 → 加根路径排除。

### R5：终态校验

- **10-项目 328 个页面 = live 项目 URL 328 条 · 孤儿 0**；`20-语料` 334 页 = 唯一条目 334 条（1:1）。
- 剩余 2 条正文缺口（betalist 1 / github 1）为 Exa 未命中的单条损耗，已记 attempts；死信账本 9 条为结构性无正文。
- 自愈工具：`kb_reclassify --repair` 修复了 12 个被合并逻辑打乱 frontmatter 的页面（**并已把该逻辑改为正规写页，不再做字符串拼接**）。

## 本轮新增/变更文件

| 文件 | 作用 |
|---|---|
| `tools/kb_backfill.py` | 粘性缺口回填（正文 + 评论，含死信账本） |
| `tools/kb_reclassify.py` | 实体错分订正 + 孤儿页合并 + frontmatter 自愈 |
| `tools/kb_analyze.py` | 画像驱动诊断 + 缺口队列 + 错误可见性 |
| `tools/kb_common.py` | 新增 `BODY_MIN`、`BodyCache` |
| `tools/kb_collect.py` | 正文/project_url 继承、dev.to 分页、人物分流、裸域名排除 |
| `_meta/channels.yaml` | 18 渠道补 `profile` 画像字段 |
| `_meta/backfill_dead.json` | 死信账本 |
| `_meta/body_cache.json` | 正文缓存 |

## 2026-09-20 修复轮（P0+P1+P2 全部执行）

**触发**：用户指出「很多内容与项目无关」「原文不全，很多是点击查看全文」，并要求全面分析其余问题。
诊断见 [[语料问题全面分析-20260920]]，结果见 [[修复报告-20260920]]。

| 项 | 修复前 | 修复后 |
|---|---|---|
| 在库语料 | 334 | **700**（近 90 天发布 590 条） |
| 可用率 | 51% | **83%** |
| 正文中位长度 | 927 | **1936** |
| Exa 8000 截断 | 17 | **0** |
| 相关性可疑 | 145 | 71 |

**动作**：停用 5 渠道（apple_rss/sspai/hn_front/lobsters/uneed，附原因+复活条件）·
新增 `_meta/rules.yaml` 主题门禁 · 新增 `kb_prune.py` 归档存量 107 条 → `80-归档/` ·
90 天历史铺底（HN 400 / GitHub 58 / Reddit 84）· 回填正文 385 条 + 评论 38 条 ·
新增 `kb_insight.py` 洞察层并写入 579 条 topic 标签。

**判据修正（本轮最有价值的部分）**：多个「语料脏」其实是**判据错** ——
契约型渠道被误判无关、正文参与主题匹配造成假阳性、信号层被用正文层尺子量、
审计读 raw 导致归档动作不可见、历史深度误用采集轴。已写进 skill「判定纪律」段。

**残留**：无指标 94 · 无项目链接 73 · HTML 残留 62 · 缺发布时间 45 ·
跨渠道强信号仅 3（PH/BetaList 的 `project_url` 结构性不可得，条目不参与归并）· 评论覆盖偏窄 · 中文语料薄。

---

## 2026-09-20 · 每日采集（run `20260920T094814`，周日）

**渠道体检**：13 个启用渠道 —— **ok 11 / error 0 / auth 2（小红书、X 待解锁）**；
启用渠道全集 18，disabled 登记 9。

> ⚠️ 本轮开工时体检 **13/13 全部 error**：`name 'links' is not defined`。
> 根因：`make_item()` 里引用了只在 `derive_project_url()` 内部定义的局部变量 `links` ——
> **所有 adapter 共用的构造路径**，故一挂全挂。属回归（上一轮修复轮改动引入）。
> 已修：`make_item` 计算 `links` 兜底，note 渲染改用**最终 body** 重算（发布站正文由 enrich 后补，
> 构造期算的外链清单恒为空）。修后试跑 6 渠道恢复正常。

**采集**：12 渠道实际取数 · 写盘 **182 条（new 23 / update 76）** · 重复率 42% ·
丢 32 条（无主题词 17 / 标题排除 15，账本 `_meta/rule_drops.jsonl`）。

**取数诊断（门禁一：抓到了吗）**：缺口 **27 条**（hn_show/body 22、github_new/body 5）
→ 回填 → **15 条**（hn_show 11、github_new 4），回填 19/40 命中，死信账本累计 51 条（判死 26）。
剩余 15 条为 Exa 未命中的单条损耗，已记 attempts。缺 url **0**。

**内容审计（门禁二：抓到的能用吗）**：在库 **710** 条 · 可用 **612（86%）** ·
不可用 53（空/过短/残文）· 相关性可疑 54。正文中位 1779。
发布轴近 90 天 596 条 · 更早 61 · 无发布时间 53。跨渠道重复 1。

| 渠道 | 可用/条目 | 处置 |
|---|---|---|
| producthunt / betalist / indiehackers / hn_show / bilibili / exa_discovery | 100% / 100% / 100% / 91% / 95% / 89% | keep |
| v2ex 83% · c1c7 86%（修复前 43%） | — | keep |
| github_new 40/57（70%） | 62% → 70% | 新增排除规则（clone/tutorial/wiki/journal/directory） |
| reddit 58/94（62%） | 50-80% 区间 | **降量 limit 40→30** |
| devto 3/5（n=5） | 小样本 | 标题门槛会**过滤成 0 条**（杀渠道），回退为少量排除 + limit 15→8 |

**本轮判据修正（又抓到三个「拿错尺子」）**：

1. **「大厂产品」假阳性**：Show HN 标题里提到 Stripe / Google / OpenAI / Microsoft 是**依赖**不是**出品方**。
   实测 hn_show 7 条「大厂产品」全是此类 → 标题自述自建项目时不再判大厂。hn_show 358 → **364**。
2. **人物名片被当项目语料**：c1c7 的 `Patrick McKenzie (@patio11)` / `Pieter Levels (@levelsio)`
   （`kind=person` → `30-人物/`）被判「无项目信号 + 不可用」，把小样本渠道压到 43% → 现在 **86%**。
   新增 `KIND_EXEMPT`，与信号层豁免同一道理。
3. **Exa 截断标记未随上限升级**：`FULLTEXT_MAX_CHARS` 已从 8000 提到 40000，
   但审计器仍写死 8000（漏报 40000 档）、`kb_backfill` 切片不写 `body_truncated`（**静默截断**）。
   两处已修，截断标签 14 条（hn_show 12 / indiehackers 2）首次如实暴露。

4. **「过滤后剩 0 条」不是提纯，是杀渠道**：devto 试加标题主题词门槛后，最新 5 条**全部**被过滤 →
   渠道直接变空。dev.to 在 showdev/sideproject 等 tag 下已经漂成「泛 dev 博客」（prompt 工程、
   周目标帖、求职经历），标题本就不带产品/变现词。已回退为「少量标题排除 + limit 15→8」。
   → 教训：**模拟过滤时必须同时看「剩余条数」**，只看可用率会把「渠道归零」误当成「质量提升」。

**存量重判**：`kb_prune --apply` 归档 13 条（github_new 9 / reddit 4 条平台删除标记）；
孤儿项目页合并后 **0 孤儿**；frontmatter 无需修复。

**质检门**：缺 url 0 ✅ · 按画像有正文 discussion 92% / linkpost 100% / project 89% / discover 100% ✅ ·
新条数 182 ∈ [100,300] ✅ · 单渠道最大占比 16% ✅ · **可用语料 86% ≥70%** ✅ ·
discussion 评论抓取：hn_show 78% / reddit 83% / devto 100% / lobsters 100% ✅ —
**唯 V2EX 0%（sov2ex 结构性不返回回复正文，已登记为已知缺口）**；合并口径 34%，剔除 V2EX 后 82%。

**异常 delta**：`jina_reader` —— agent-reach doctor 声明 `web=ok`，实测 `<urlopen error timed out>`
（连续多轮复现），已登记 `backends` 段 `status: unusable`，通用正文唯一后端仍是 Exa web_fetch。

**渠道增删决定**：**新增候选 2 个（写入 `_meta/channel_candidates.yaml`，均已完成取数实测、尚未接入）**① `w2solo` 中文独立开发者社区（首页静态 HTML 46 个 `/topics/` 链接；无 JSON API/RSS → 需新 adapter，补中文讨论语料，zh 现仅 76/710）；
② `wonderlaunch`（solo founder 发布站，首页静态 HTML 12 个 `/product/` 链接；无 feed；只收人工验证的独立开发者产品）。
**未启停任何现有渠道**；新增 `content_gate` 段把可用率证据固化进 `channels.yaml`（单一事实源）。

**新增工具**：`tools/kb_healthcheck.py` —— 三组等式自检（`20-语料 == live 条目`、应有项目页缺页 0、缺 url 0）。
本轮结果：710 == 710 ✅ · 缺页 0 ✅ · 缺 url 0 ✅。
（注：不要用「`10-项目` 文件数 − live `project_url` 去重数」当孤儿判据 —— 归档条目旧页不删，
会报出 260+ 假孤儿；须按文件名逐个匹配 live 条目。）




## 2026-09-20 15:00 · project_url 坏值收尾 + 订正遗留孤儿清理

**背景**：`--project-url` 订正 47 条后，最新记录已全部干净（错值仅残留在 raw 历史行，
append-only 不改属正常）。但订正改变了 project_url → 实体页文件名与 project_ish 判定，
在 `10-项目/` 留下两类孤儿（690 页 vs 应有 622）。

**处理**（全部走脚本，未手改）：
1. **4 对坏值孤儿**（liqi.io/creators: ×2、localhost:8080 ×2）：观测历史并入规范页后删除
   （Arnis、Clickydots、pritime、r20-quantum-trader）。
2. **16 对改名孤儿**（bilibili 视频链接 / reddit 图片页 / dev.to 文章页等被 project_url_reject
   拒绝后改名或转 kind）：同 title 配对规范页，合并观测历史后删除。
3. **56 个 stale 页**：`kb_prune --liveness --apply` 打标 → `kb_moc`（先重建地图，否则被地图
   链接拦下）→ `--archive-entities --apply` 移出 28 个（manifest：
   `_meta/entity_archive_manifest_20260920T144308.json`）；
   剩 28 个被 `归档与申诉.md` 申诉抽样段**设计内引用**拦下（捞回入口需要它们，宁留不断链）。

**结果**：项目页 690→623（应有 622，+1 差额=申诉可达的 stale 页）。八项不变量全绿
（①710=710 ②缺页0 ③缺url0 ④断链0 ⑤漂移0 ⑥孤立0 ⑦可疑pu=0 ⑧共用4组正常）。

**修复 `kb_name_audit` 自引用怪圈**：报告曾把同 stem 违规写成 `[[页名]]`，报告自身成为
引用方，永久拦住 `--archive-entities` 搬走被点名的页。已改为纯文本路径，第二轮归档随之
清掉 23 页（累计归档 51 页，manifest：`...T144308` / `...T144809`）。

**最终审计状态（中风险 11 处，均设计内/既有，非违规）**：
- 5 处「实体页无在库条目指向」：被 `归档与申诉.md` 申诉抽样段引用的 stale 页（捞回入口，宁留不断链）；
- 6 处「同 stem 多目录（短链歧义）」：归档区与在库同名条的既有形态，非本轮引入。

**工具面**：`repair_project_url` 已加「错值无论有无正文都订正」gate；后续订正后必须按
周流程 `liveness --apply → kb_moc → archive-entities`，顺序不能跳。
## 2026-09-20 15:20 · §1 评论账本修复闭环 + 回填引发的实体页收尾

**§1 评论账本（修复+回填+验证）**
- 修复：_enrich_hn_comments 平台计数兜底（comments_total = max(平台, 实抓)）+ 单条失败不拖垮整批 + 失败留痕 xtra.comments_error；comments_gap 去掉 ≥20 门槛改为 platform > grabbed；回填写盘重算 body_completeness。
- 回填：hn_show 评论 113 条全补、reddit 55 条全补；二轮又补 hn 7 + reddit 28；github readme 8 条。
- 效果：hn_show 带评论 31→148（覆盖率 97%，应有 152）；reddit 88/91=97%；devto 100%。**残留 7 条（hn 4 + reddit 3）已在死信账本 attempts=2** —— 平台计数含已删除/灰化评论，API 返回空，属结构性不可得，按设计走死信流程。
- 分析报告评论口径已改：覆盖 = 有评论/应有评论，抓取深度 只在抓到过的条目里算（防止单看高估）。
- 语料 710→713：+3 为本轮回填/订正合法恢复的条目（repair/update 记录翻转状态，健康检查 ① 双向闭环 713=713，无归档冲突）。

**回填连带：实体页收尾序列再跑一轮**
- liveness 标记 stale 23 → kb_moc 重建（地图 621 页全关联）→ archive-entities 搬出 18 页（manifest: entity_archive_manifest_20260920T151726.json）→ kb_insight --browse（在库 713 · 可用 620 · 项目型 554）。
- 实体页 644→626（应有 625，+1 为申诉可达 stale 页）。

**收工门**：健康检查八项全绿（①713=713 ②缺页0 ③缺url0 ④断链0 ⑤漂移0 ⑥孤立0 ⑦可疑pu=0 ⑧共用4组正常）；命名审计 17 处（12 短链歧义 + 5 stale 实体页，均设计内：申诉入口可达性）；自测 15/15 OK。

**§1 至此闭环。** 剩余大项：§9-11 工程面（git init / 测试扩面 / 清 kb_qc.py 与 __pycache__ / 状态文件保留策略）。

## 2026-09-20 15:35 · 正文完整性系统排查（专项）

对「语料是全文还是摘要/首段」做了全面核对（live 713 条逐条）：
- **可用率 87%**（正文层 694 条中可用 622，信号层 19 条豁免完整性判定）。
- **真截断仅 14 条**：撞 Exa 40000 上限 10 + 旧 8000 上限残留 4，全部记录在案（长文只入前 40k 字符，对分析够用，边界明示）。
- **RSS 摘要残留 9 条**（github 4 / hn 3 / reddit 2）→ 已在回填队列。
- **正文过短(<200) 32 条**：bilibili 10（signal 层设计内）、github 短 readme 5、其余为无 story_text 的 hn 帖。
- **空正文 20 条**（hn 15：YouTube/需登录/Exa 未命中，均带 fulltext_url 已在回填/死信账本）。
- **长度分布反证非首段**：中位 1845、p75 6047、p90 12626 字符 —— 首段通常 200-500 字，分布不支持「只抓首段」假设。
- 自写 no_tail_punct 启发式（237 条命中）经抽验为误报（代码/markdown 正文天然以 </script>/表格/HTML 结尾），官方判定以 SUMMARY_TAIL 正则 + 精确撞顶为准。
- 内容审计报告已重写刷新（00-索引/报告/内容审计-all.md）。

## 2026-09-20 22:50 · 工程面集中收尾（git 入库 + 工具链重构 + 自测扩面）

**触发**：全库工程审计（分析见会话）——P0 无版本控制/测试薄、P1 采集器巨石与吞异常、P2 _meta 膨胀。
全部改动只动 `tools/` 与索引层文档，未触碰任何生成页与 `90-原始/`。

| 项 | 之前 | 之后 |
|---|---|---|
| 版本控制 | 无 git（删除不可恢复） | **git init + GitHub `KOIYC/CollectProjectAndDetails`（main，已推送）**；`.gitignore` 排除 `__pycache__/`、`.workbuddy/`、`.obsidian/workspace*.json` |
| 遗留探针 | `kb_qc.py` + `__pycache__`（312/313 双版本） | 已删（commit f15cbb0） |
| kb_collect.py | 1644 行巨石（adapter/写页/管线耦合） | **拆出 `kbc_channels.py`**（15 adapter + enricher + project_url 守卫，只依赖 kb_common）；kb_collect 以 facade re-export，9 个调用点 import 不变（commit c45af3a） |
| 注册表解析 | mini-yaml 在采集器里，其他工具反向 import kb_collect | `load_channels_yaml` / `load_registry` 迁入 **kb_common** |
| 状态文件损坏 | `_load_json` 静默返回默认值（seen/body_cache 损坏=整库失忆无人知） | **损坏隔离**：改名 `.corrupt-<ts>.bak` 留现场 + 响亮告警，从零重启 |
| 写盘失败 | 只进 stderr，run JSON 无指标 | 计数进 run JSON `write_errors` + 收尾响亮汇总；**退出码有意恒 0**（单条失败已隔离且 raw 落盘可重放，非零会断自动化链） |
| 显式窗口 | 只能按渠道 window_days | `--since/--until`（窗口自动前推 + published_at 裁剪；HN 的 until 直进 Algolia numericFilters）；修了 `--days` 对 params.window_days 渠道无效的旧问题 |
| 限流节流 | 无 | `--throttle-ms`（默认 25ms/请求，作用评论抓取四渠道） |
| project_url 校验 | 拒绝尾部标点，但 `liqi.io/creators`（无冒号）能过 | 新增 **profile 路径拒绝**（creators/users/u//profile/people/@handle）；对 516 个存量 live project_url 零误伤（healthcheck ⑦ 仍绿） |
| _meta 保留策略 | manifest/run 记录只增不减（单日 5+ manifest、36+ runs） | `rotate_files(*_manifest_, 12)` / `rotate_runs(80)` 接线 prune/navfix/backfill/reclassify + RunLog；删除份由 git 历史兜底 |
| 自测 | 15 例 | **34 例**：BodyCache（只增不减/损坏隔离）、Seen 账本、make_item 四态、mini-yaml 子集、frontmatter 读写、轮转、显式窗口裁剪 |

**决策记录（新增一条）**：判定常量（BODY_MIN/SUMMARY_TAIL/KIND_EXEMPT 等）**留在 kb_common 不外置 rules.yaml** ——
rules.yaml 只放「主题准入词表」这类运营可调项；判定常量与消费它的代码必须同版本演进，外置反增漂移面。

**验证**：全 16 模块导入 OK · 自测 34/34 · healthcheck 八项全绿（①713=713 ②缺页0 ③缺url0 ④断链0 ⑤漂移0 ⑥孤立1(既有软门) ⑦可疑0 ⑧共用4组正常）· 命名审计 17 处（均设计内既有）· `--since/--until` 单渠道 dry-run 端到端通过。

**commit 链**：`432c08d` 初始 → `f15cbb0` 清理 → `c45af3a` 拆分 → `020bb9f` 测试/轮转 → 本次文档。
**遗留**（非本轮工程债）：w2solo/wonderlaunch 接入、V2EX 评论 0%（sov2ex 结构性）、40-方法论人工沉淀、P0 解锁小红书/X。

---

# 修复轮（2026-09-21 跨零）· 五个静默 bug + 结构债清零

**触发**：全库问题分析 → 用户「执行全部修复」。本轮修的是**会咬人的工程 bug**，不动生成页/`90-原始/`。

| # | bug | 症状 | 根因 | 修复 |
|---|---|---|---|---|
| 1 | 轮转排序错 | 自测 `test_rotate_files_keeps_newest` 挂：保留旧 3 份、删了最新 | `rotate_files`/`rotate_runs` 按 `st_mtime` 排序，紧密循环分辨率不够、任何 touch 会骗过它 | 改按**文件名**（内嵌 YYYYMMDDTHHMMSS，字典序即时间序）排序；加回归自测钉死 |
| 2 | fix-names 越冻结区 | 归档区 31 个反算改名，会把 14 个 method 归档副本改成与 `40-方法论/` 在库页**同名** | `NAME_DIRS` 含 80-归档，对归档页也做规范名反算，违反「归档=冻结快照」决策 | 归档区只允许两类改名：**语法字符清理** + **撞名消歧**（`-dupN`，不登记进 pairs，`[[短名]]` 唯一指向在库页）；再加「归档内部跨分片撞名」消歧 |
| 3 | prune 覆盖冻结区 | `-dup` 单发后缀被占时 `shutil.move` **静默覆盖**已存在的 `-dup`（烧历史） | 归档→回填→再归档链造出 `-dup` 链，单发后缀会撞 | 新增 `_free_dup_name` 顺延 `-dup/-dup2/-dup3…`；两处 apply 都接；加自测 |
| 4 | backfill 复活归档 + 分片迁移造孤儿 | 45 个孤儿语料（healthcheck ① 红 752≠707）；归档条目被回填写回在库区 | ① backfill 不滤归档条目 → 复活冻结区条目；② 回填记录追加进「今天」raw 分片，跨天后 `load_latest` 把条目 day 解析成回填日 → note 迁移新分片、旧文件成孤儿 | ① `build_queue`/`build_comments_queue` 跳过 `seen.note` 在 `80-归档/` 的条目；② 新增 `kb_common.note_bucket`：note 原地刷新，分片日期以现存 note 为准（kb_collect 跨天 update 同用）；45 孤儿送回收站（git + raw 双备份） |
| 5 | 孤立语料无自愈 | healthcheck ⑥ 卡 1 条：v2ex `e2893ab6`（同 project_url 两条目，后到者建页时从未追加自己的观测行） | 项目页 writer 只给「触发建页的那条」加行，同项目兄弟条目成孤岛 | `reconcile_entities` 补第③段：遍历在库应建页条目，其项目页 obs 缺自己语料链接就走 `write_entity_note` 正规补行（幂等） |

**顺带结构维护**：navfix 补 72 条过期导航段；存量重判归档 6 条（rules.yaml 已含 moderator/removed 排除，孤儿页合并 0）；回填两轮 29 正文 + 67 评论；liveness 标 stale 25 → kb_moc 更图 → archive-entities 搬 25 → insight 刷新 `洞察-20260921` → 搬家后 `fix-links` 改写 253 链接。

**验证**：自测 **34→38 例全过** · healthcheck **八项全绿**（①707=707 ②缺页0 ③缺url0 ④断链0 ⑤漂移0[含盘↔账孤儿0/幽灵0] ⑥**孤立0** ⑦可疑0 ⑧共用4组正常）· **命名审计清零**（17→0，本轮消歧 + 归档内跨分片消歧）· 内容审计可用率 87%→**88%**（Exa 截断 17→14、RSS 残文 31→7 已在上轮，本轮无回归）。

**决策记录（新增）**：① 轮转/回填/搬家一律**按文件名（内嵌时间戳）排序**，不信 mtime；② 语料分片日期 = 条目**首次落盘日**，raw 分片日期 = 记录进账日，两者语义不同，更新/回填**原地刷新不迁移**；③ 冻结区改名只走「清字符 + 撞名消歧」两条，绝不做规范名反算。
